PORT=3000
DATABASE_URL=postgresql://user:password@localhost:5432/deped_school
JWT_SECRET=
DATA_RESIDENCY_REGION=ph

# Field-level AES-256 encryption key (TweetNaCl) — for sensitive columns beyond
# password hashing (bcrypt, already applied to users.password_hash). Not yet
# wired into any query; generate and set before that work begins.
ENCRYPTION_KEY=
